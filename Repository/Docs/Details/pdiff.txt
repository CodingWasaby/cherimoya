diff和pdiff的使用方法和功能完全相同。唯一的区别是在表达式步骤显示中,diff表示为dy/df，而pdiff表示为∂ y/∂ x。
@{ex}
@ f=diff(@x -> 2*pow(x, 2), "x")
第2个参数表明对哪个变量求导。如果被导函数是一元函数，仍需写明自变量名称。
返回值为表达式类型，一般用于求值函数eval或evald中根据输入变量求值。
@{ex}
@ d=eval(f, {x:5})